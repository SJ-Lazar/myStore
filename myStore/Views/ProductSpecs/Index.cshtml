@model IEnumerable<myStore.Models.ProductSpec>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProductId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CategoryId)
            </th>
         
          
            <th>
                @Html.DisplayNameFor(model => model.TagId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BrandId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ProductId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CategoryId)
            </td>          
            <td>
                @Html.DisplayFor(modelItem => item.TagId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BrandId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ProductSpecId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ProductSpecId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ProductSpecId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
